{
  "name": "tessera-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "test": "turbo run test",
    "registry:build": "node scripts/build-registry.mjs",
    "llms:build": "node scripts/build-llms.mjs"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=18.18"
  }
}
